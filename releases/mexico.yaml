apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: "mexico"
  namespace: "mexico"
spec:
  releaseName: "mexico"
  chart:
    git: git@github.com:michaelperel/config
    path: charts/helloworld
    ref: master
  values:
    COUNTRY: "mexico"
